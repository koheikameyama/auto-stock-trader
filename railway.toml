[build]
builder = "dockerfile"
watchPatterns = [
  "src/**",
  "prisma/**",
  "yfinance-service/**",
  "scripts/start.sh",
  "package.json",
  "package-lock.json",
  "tsconfig.json",
  "Dockerfile",
]
